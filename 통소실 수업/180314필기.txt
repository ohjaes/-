%% : 나머지 구할때 쓰는거
&,| : 모든 원소 참 거짓 연산 비교
&&, || : 벡터의 첫번째 원소만 참 거짓 연산 비교

비교연산자는 수치에서 쓴다
비교연산자의 ==,와 !=는 문자 논리 수치등 다 쓸수있다.

all(a==b) a와 b의 모든 벡터의 원소들의 연산이 트루면트루
 any() 는 하나라도 맞으면 트루

중간고사는 코드보고 결과적고 결과보고 코드적고 코드로
짜는거 코드 중간에 빈칸등등 컴퓨터 안사용하고 페이퍼시험

연산자 우선순위 외우기 (ln2 19page)

r에서 log는 자연로그가 디폴트임
상용로그는 log10()으로 쓸수있고 나머지는 log(10,base=x)

var(x)는 표본분산을 계산해준다

order(x)는 정렬하기 전 x의 원소들의 원래 위치

r에서는 17과 17+0i는 완전히 달라서 복소수로 쓸거면 복소수로
써줘야된다 complex()로 써도 되고 a+bi처럼 써도되고

sqrt(-17)은 에러 sqrt(17+0i)로 써야됨

복소수 연산 공부하기

seq.int(from, to, by, length.out, along.with, ...)이게
생략했을 때 seq함수의 기본 순서

length()는 함수로서 벡터의 길이를 알려줌
seq(length=?)처럼 함수 내에 있는 length는 인수일뿐
length=3처럼 변수로 그냥 쓴 length라는 이름일뿐
사용자가 변수이름으로 정의하면 원래있던 함수보다
사용자가 변수로 정의한게 우선순위다

practice는 r코드를 짜놓고 가지고 있어라 불시에 검사한다

vec <- vector() 이건 비어있는 벡터생성으로 vec을 벡터
타입으로 만드려고 쓰는거, 비어있는 벡터는 제일 약한 
논리타입으로 되어있다. 논리가 젤 바꾸기 쉽고 싸우면 져서

벡터도 딕셔너리처럼 인덱스의 이름을 줄수있음
