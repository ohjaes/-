bit
byte=8bit        char는 1바이트  한글은 2바이트

정수형은 4바이트=32비트

메모리주소를 32비트로 하면 32비트 64비트로 하면 64비트

float 4byte
double 8byte

r에서는 문자열만 써서 큰따옴표 작은따옴표 상관 없다

logical 타입 많이쓴다

TRUE, FALSE 다 대문자로 써야된다 T,F로 써도 상관없다

NA=Not Available  모르겠다라는 뜻

as.함수 =변환
is.함수 =판단

mode는 원래 통계에서 최빈값인데 r에서는 유형확인함수
r은 최빈값 함수 없다 직접 짜야된다.

정수형은 딱 2가지(as.integer와  3:5처럼 시퀀스로 1씩 증가하는거)

**하나의 벡터에는 같은 형태의 값을 가져야한다. 다르면 자동변환 됨

r은 정보손실이 없는 방향으로 움직인다. 문자로 바꿔주는건 ""만 하면되서 좋다.

**길이가 다른 벡터의 연산은 길이가 짧은 벡터의 원소가 순서대로 반복하여
계산을 한다. 긴 벡터가 짧은 벡터의 상수배와 같으면 warning메시지 안뜬다.
